import React from 'react';
import '../css/indexingComplete.css'; // Ensure to import your CSS file if needed

const IndexingComplete = () => {
    return (
        <div className="indexing-complete-container">
            <div className="indexing-complete-box">
                <p className="indexing-complete-message">Indexing process has been completed successfully.</p>
            </div>
        </div>
    );
};

export default IndexingComplete;
